'use strict';

/**
 * characteristic-bundle router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::characteristic-bundle.characteristic-bundle');
